/*
 * This file was generated by the OEM can_api (/common/can_api/c_generator.py) from the c_file.j2 Jinja2 template.
 */

#include "{{ header_file }}"
#include "stm32g4xx_hal_fdcan.h"

/*
 * Declare handle for FDCAN1 peripheral
 */
FDCAN_HandleTypeDef hfdcan1; 

/*
 * Create an empty cantools struct for each message to be received
 */
{% for message in rx_messages %}

struct can_tools_{{ message.name|lower }}_t {{ message.name|lower }};

{% endfor %}

/*
 * Create an empty cantools struct and TX header for each message to be transmitted
 */
{% for message in tx_messages %}

FDCAN_TxHeaderTypeDef sTxHeader_{{ message.name|lower }};
// volatile qualifier is used, as the value of this struct may change outside
// of the standard program flow. Prevents compiler optimizations that could
// break code.
volatile struct can_tools_{{ message.name|lower }}_t {{ message.name|lower }}; 

{% endfor %}

/*
 * @brief Configure the FDCAN1 initialization struct.
 *
 * Set the options for how we want the CAN peripheral to behave.
 */
static void can_config(void){
    hfdcan1.Instance = FDCAN1; // Use FDCAN1
    hfdcan1.Init.ClockDivider = FDCAN_CLOCK_DIV1; // Divide kernel clock by 1 (no divider)
    hfdcan1.Init.FrameFormat = FDCAN_FRAME_CLASSIC; // Use Classic CAN frame standard
    hfdcan1.Init.AutoRetransmission = DISABLE;
    hfdcan1.Init.TransmitPause = DISABLE;
    hfdcan1.Init.ProtocolException = DISABLE;
    hfdcan1.Init.NominalPrescaler = 1; // CAN bit timing. See documentation
    hfdcan1.Init.NominalSyncJumpWidth = 2; // CAN bit timing. See documentation
    hfdcan1.Init.NominalTimeSeg1 = 27; // CAN bit timing. See documentation
    hfdcan1.Init.NominalTimeSeg2 = 4; // CAN bit timing. See documentation
    hfdcan1.Init.DataPrescaler = 1; // Not used in Classic CAN
    hfdcan1.Init.DataSyncJumpWidth = 1; // Not used in Classic CAN
    hfdcan1.Init.DataTimeSeg1 = 1; // Not used in Classic CAN
    hfdcan1.Init.DataTimeSeg2 = 1; // Not used in Classic CAN
    hfdcan1.Init.Mode = FDCAN_MODE_NORMAL; // Normal operating mode
    hfdcan1.Init.StdFiltersNbr = {{rx_messages|length}}; // Number of (standard length) message ID filters
    hfdcan1.Init.ExtFiltersNbr = 0; // Number of (extended length) message ID filters
    hfdcan1.Init.TxFifoQueueMode = FDCAN_TX_FIFO_OPERATION; // Use FIFO mode
}

/*
 * @brief Configure RX filters
 *
 * For each message, first configure the reception (RX) filters to defines 
 * which message IDs we care about and should recieve. Then apply configured
 * filter to CAN peripheral.
 *
 * @returns 0 for success, 1 for error
 */
static HAL_StatusTypeDef config_rx_filters(void) {
    HAL_StatusTypeDef status = HAL_OK;

    {% for message in rx_messages %}

    // Filter config for: {{ message.name|lower }}
    FDCAN_FilterTypeDef sFilterConfig_{{ message.name|lower }};
    sFilterConfig_{{ message.name|lower }}.IdType = FDCAN_STANDARD_ID;
    sFilterConfig_{{ message.name|lower }}.FilterIndex = {{ loop.index0 }}; {# Current index within Jinja loop, starting at 0 #}
    sFilterConfig_{{ message.name|lower }}.FilterType = FDCAN_FILTER_MASK; // Classic filter: FilterID1 = filter, FilterID2 = mask
    sFilterConfig_{{ message.name|lower }}.FilterConfig = FDCAN_FILTER_TO_RXFIFO0; // Store message in Rx FIFO 0 if filter matches
    sFilterConfig_{{ message.name|lower }}.FilterID1 = {{ message.frame_id }}; // Message ID
    sFilterConfig_{{ message.name|lower }}.FilterID2 = 0x7FF; // Bitmask set to all 1s -> only accept exact ID match
    status = HAL_FDCAN_ConfigFilter(&hfdcan1, &sFilterConfig_{{ message.name|lower }});
    if (status != HAL_OK) {
        // Filter config failed, exit loop
        return status;
    }

    {% endfor %}

    return status;
}

/*
 * @brief Configure TX headers
 *
 * The TX header is the supporting information for each CAN message we hope to
 * send, but does not include actual data.
 */
static void config_tx_headers(void){
    {% for message in tx_messages %}

    sTxHeader_{{ message.name|lower }}.Identifier = {{ message.frame_id }};
    sTxHeader_{{ message.name|lower }}.IdType = FDCAN_STANDARD_ID;
    sTxHeader_{{ message.name|lower }}.TxFrameType = FDCAN_DATA_FRAME;
    sTxHeader_{{ message.name|lower }}.DataLength = FDCAN_DLC_BYTES_{{ message.length }};
    sTxHeader_{{ message.name|lower }}.ErrorStateIndicator = FDCAN_ESI_ACTIVE;
    sTxHeader_{{ message.name|lower }}.BitRateSwitch = FDCAN_BRS_OFF;
    sTxHeader_{{ message.name|lower }}.FDFormat = FDCAN_CLASSIC_CAN;
    sTxHeader_{{ message.name|lower }}.TxEventFifoControl = FDCAN_NO_TX_EVENTS; // We do not use the TX events feature
    sTxHeader_{{ message.name|lower }}.MessageMarker = 0; // Ignored because of FDCAN_NO_TX_EVENTS

    {% endfor %}
}

/*
 * Initialize the FDCAN1 module.
 */
int can_init_{{ node.name }}(void) {
    can_config();
    if (HAL_FDCAN_Init(&hfdcan1) != HAL_OK) {
        // Init failed
        return 1;
    }
    if (config_rx_filters() != HAL_OK){
        // RX filter config failed
        return 1;
    }
    config_tx_headers();
    if (HAL_FDCAN_Start(&hfdcan1) != HAL_OK){
        // Start failed
        return 1;
    }
    return 0;
}

/*
 * Receive messages
 */
int can_poll_receive_all(void) {
    FDCAN_RxHeaderTypeDef RxHeader;
    uint8_t RxData[8]; // 8 data bytes in Classic CAN frame

    // Keep reading as long as there are messages in the FIFO
    while (HAL_FDCAN_GetRxFifoFillLevel(&hfdcan1, FDCAN_RX_FIFO0) > 0) {
        if (HAL_FDCAN_GetRxMessage(&hfdcan1, FDCAN_RX_FIFO0, &RxHeader, RxData) == HAL_OK) {
             // Unpack message into the correct struct based on message ID
            switch (RxHeader.Identifier) {
                {% for message in rx_messages %}
                case {{ message.frame_id }}:
                    {
                    // We protect the following in an 'atomic' block to prevent data from changing while unpacking
                    uint32_t primask_bit = __get_PRIMASK(); // Get current interrupt state
                    __disable_irq(); // Disable interrupts
                    // CRITICAL SECTION BEGIN
                    can_tools_{{ message.name|lower }}_unpack(&{{ message.name|lower }}, RxData, (uint8_t)RxHeader.DataLength);
                    // CRITICAL SECTION END
                    __set_PRIMASK(primask_bit); // Set interrupts back to prior state
                    }
                    break;
                    
                {% endfor %}
            }
        } else {
            // Failed to get message
            return 1;
        }
    }
    return 0;
}

/*
 * Transmit messages
 */
{% for message in tx_messages %}

int can_send_{{ message.name|lower }}(void) {
    uint8_t TxData[8] = {0};

    // We protect the following in an 'atomic' block to prevent data from changing while packing
    uint32_t primask_bit = __get_PRIMASK(); // Get current interrupt state
    __disable_irq(); // Disable interrupts
    // CRITICAL SECTION BEGIN
        can_tools_{{ message.name|lower }}_pack(
            TxData,
            // We can safely discard the volatile qualifier because we are in an
            // ATOMIC block, so the value will not be changed in an ISR
            (const struct can_tools_{{ message.name|lower }}_t*) &{{ message.name|lower }},
            {{ message.length }}
        );
    // CRITICAL SECTION END
    __set_PRIMASK(primask_bit); // Set interrupts back to prior state


    if (HAL_FDCAN_AddMessageToTxFifoQ (&hfdcan1, &sTxHeader_{{ message.name|lower }}, TxData) == HAL_OK) {
        return 0;
    } else {
        return 1;
    }
}

{% endfor %}
